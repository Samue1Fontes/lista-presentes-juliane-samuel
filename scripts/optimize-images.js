// This is an ES module stub so ESLint won't error on `require` usage in scripts.
export function noop() {
  // noop
}

console.log('ESM stub present — use optimize-images.cjs to run the optimizer');
